---
apiVersion: v1
kind: Namespace
metadata:
  name: minns

---
apiVersion: v1
kind: Service
metadata:
  name: min-hsvc
  labels:
    name: min-hsvc
  namespace: minns
spec:
  clusterIP: None
  selector:
    name: min-pod
  ports:
    - name: minio-data
      port: 9000
      targetPort: 9000
    - name: minio-console
      port: 9090
      targetPort: 9090
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: min-sts
  labels:
    name: min-sts
  namespace: minns
spec:
  replicas: 4
  serviceName: min-hsvc
  selector:
    matchLabels:
      name: min-pod
  volumeClaimTemplates:
    - apiVersion: v1
      kind: PersistentVolumeClaim
      metadata:
        name: data-vol
        labels:
          name: data-vol
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: do-block-storage-xfs
        resources:
          requests:
            storage: 1Gi
  template:
    metadata:
      name: min-pod
      labels:
        name: min-pod
    spec:
      initContainers:
        - name: min-init-container
          image: busybox
          command:
            - /bin/sh
            - -c
            - "chown 1001 /data"
          volumeMounts:
            - name: data-vol
              mountPath: /data
      containers:
        - name: min-container
          image: bitnami/minio
          imagePullPolicy: IfNotPresent
          command:
            - minio
            - server
            - --address=:9000
            - --console-address=:9090
            - http://min-sts-{0...3}.min-hsvc.minns.svc.cluster.local/data
          env:
            - name: MINIO_ROOT_USER
              value: rootroot
            - name: MINIO_ROOT_PASSWORD
              value: Y2hhbmdlaXRvbWc=
          ports:
            - name: http
              containerPort: 9000
              protocol: TCP
            - name: console
              containerPort: 9090
              protocol: TCP
          volumeMounts:
            - name: data-vol
              mountPath: /data
          livenessProbe:
            httpGet:
              path: /minio/health/live
              port: 9000
          readinessProbe:
            httpGet:
              path: /minio/health/ready
              port: 9000
          resources:
            requests:
              cpu: 200m
              memory: 250M
---
apiVersion: v1
kind: Service
metadata:
  name: min-svc
  labels:
    name: min-svc
  namespace: minns
spec:
  type: LoadBalancer
  selector:
    name: min-pod
  ports:
    - name: data
      port: 9000
      targetPort: 9000
    - name: console
      port: 9090
      targetPort: 9090

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: min-ing
  labels:
    name: min-ing
  namespace: minns
spec:
  ingressClassName: nginx
  rules:
    - host: data-146.190.202.84.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: min-svc
                port:
                  name: data
    - host: console-146.190.202.84.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: min-svc
                port:
                  name: console
